imports
imports
imports

Fly wheel motor;
kick wheel motor;
hood wheel motor;


left bumper = fly wheel motor on
right bumper = kick wheel motor on


kick wheel motor = once left bumper pressed activate for 2 seconds 
fly wheel motor = once right bumper pressed activate for 4 seconds

if fly wheel motor activated, activate hood wheel motor;
hood wheel motor activates for 4 seceonds 1 second after fly wheel motor;


import 
import
import

Motor flyWheel;
Motor kickWheel;
Motor hoodWheel;

if(gamepad2.left_bumper){
    flyWheel.setPower(1);
    sleep: 4000 milliseconds;
 } 

 if (gamepad2.right_bumper){
    rightBumperMethod();
}

public static void rightBumperMethod(){
    kickWheel.setPower(1);
    sleep: 2000 milliseconds;

    hoodWheel.setPower(1);
    sleep: 2000 milliseconds
}
